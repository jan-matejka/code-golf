services:
  hello-world-c:
    build:
      context: ./
      dockerfile: hello-world/c/Containerfile
      args:
        COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME}
    volumes:
      - ./hello-world/c/:/app
      - ./common.mk:/common.mk
    x-podman.uidmaps:
      - +1000:@1000
