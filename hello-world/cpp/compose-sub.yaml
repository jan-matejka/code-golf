services:
  hello-world-cpp:
    build:
      context: ./
      dockerfile: hello-world/cpp/Containerfile
      args:
        COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME}
    volumes:
      - ./hello-world/cpp/:/app
      - ./common.mk:/common.mk
    x-podman.uidmaps:
      - +1000:@1000
