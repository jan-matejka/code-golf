services:
  hello-world-cpp:
    image: localhost/code-golf_hello-world-cpp:latest
    build:
      context: ./
      dockerfile: hello-world/cpp/Containerfile
      args:
        COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME}
    volumes:
      - ./hello-world/cpp/:/app
      - ./common.mk:/common.mk
    userns_mode: "keep-id:uid=1000,gid=1000"
