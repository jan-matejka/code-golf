services:
  hello-world-python:
    build:
      context: ./
      dockerfile: hello-world/python/Containerfile
      args:
        COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME}
    volumes:
      - ./hello-world/python/:/app
      - ./common.mk:/common.mk
    userns_mode: "keep-id:uid=1000,gid=1000"
